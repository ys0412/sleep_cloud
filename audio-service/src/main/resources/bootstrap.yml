spring:
  application:
    name: audio-service  # 应用名（作为 data-id）
  cloud:
    nacos:
      config:
        server-addr: 192.168.18.165:8848   # Nacos 配置服务地址
        namespace: public                   # 配置的命名空间
        group: sleep_cloud                  # 配置分组
        file-extension: yaml                # 文件扩展名
